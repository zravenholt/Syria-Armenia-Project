import styled from 'styled-components';

const Interpreter = styled.div`
  position: relative;
  margin-bottom: 50px;
  width: 60%;
  z-index: 1;
  background-color: white;
  border-radius: 5px;
  display: inline;
`;

export default Interpreter;
