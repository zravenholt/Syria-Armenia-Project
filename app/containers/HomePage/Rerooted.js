import styled from 'styled-components';

const Rerooted = styled.img`
  position: relative;
  display: block;
  margin: auto;
  width: 800px;
  height: 600px;
  z-index: 3;
`;

export default Rerooted;
