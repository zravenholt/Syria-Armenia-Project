import styled from 'styled-components';

const TextWrapper = styled.div`
  text-align: left;
  margin: auto;
  width: 80%;
  font-size: 20px;
  font-family: Georgia;
  z-index: 3;
`;

export default TextWrapper;
