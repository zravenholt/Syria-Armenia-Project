import styled from 'styled-components';

const SmallLocation = styled.div`
  position: relative;
  display: block;
  margin: auto;
  width: 100%;
  color: black;
  font-size: 20px;
  font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
  z-index: 1;
`;

export default SmallLocation;
